<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.milan.izrada">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.hardware.camera" android:required="true"/>

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">

        <activity android:name=".Home" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".GridHome">

        </activity>

        <activity android:name=".GesturesHome">

        </activity>

        <activity android:name=".FragmentHome">

        </activity>

        <activity android:name=".WebViewHome">

        </activity>

        <activity android:name=".ListViewHome">

        </activity>

        <activity android:name=".ImageCapture">

        </activity>

    </application>

</manifest>
